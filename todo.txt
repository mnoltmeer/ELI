[-] сделать библиотеку дл€ работы с потоками, чтобы определенный фрагмент кода
    выполн€лс€ в отдельном потоке

[-] сделать библиотеку по работе с объектами Stream
    (TFileStream, TMemoryStream, TStringStream)

[?] сделать бибилиотеку дл€ работы с сущност€ми (Entity)
    —ущность - это транспортабельна€ единица данных, в которую могут входить
    одна или несколько переменных, объектов или процедур.
    ѕредставл€ет собой два файла: бинарник с данными и описатель с кодом ELI

[-] добавить возможность запуска функций из dll (не оберток)
    ELI должен вызвать функцию с определенным набором параметров
    и вернуть результат в скрипт

[-] нова€ директива #protect{<некоторый код>}
    код скрипта в этой секции будет выполнен с гарантией от краха трансл€ции
    реализовать с помощью блоков try{}catch() с логированием исключени€
    и продолжением трансл€ции

[+] переделать логирование, все логи (и трансл€ции и исключений)
    должны сохран€тьс€ в каталог пользовател€ ѕ , чтобы избежать проблем с доступом

[-] добавить функцию _sleep(pMiliSec) котора€ приостанавливает трансл€цию
